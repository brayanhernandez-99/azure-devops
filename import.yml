
################## Import RDS ##################

# cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-rds

# terragrunt init -reconfigure
# terragrunt state list -no-color
# terragrunt import aws_rds_cluster.default invictus-db


# terragrunt import \
#   'aws_rds_cluster_instance.rds_cluster_instances["0"]' \
#   invictus-instance-db

# terragrunt import \
#   'aws_rds_cluster_instance.rds_cluster_instances["1"]' \
#   invictus-instance-db-reader


# terragrunt plan --all --non-interactive -no-color
# terragrunt apply --all --non-interactive -no-color
