jobs:
  - ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/master') }}:
      - ${{ each region in parameters.red }}:
          - template: ./job/job-fronted.yml
            parameters:
              region: ${{ region }}
              red: ${{ parameters.red }}
              apps: ${{ parameters.apps }}

  - ${{ if ne(variables['Build.SourceBranch'], 'refs/heads/master') }}:
      - template: ./job/job-fronted.yml
        parameters:
          red: antioquia
