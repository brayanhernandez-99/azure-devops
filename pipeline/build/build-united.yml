stages:
- stage: 'build_unit'

  variables:
  - template: ../variables/groups-transversal-variables.yml

  jobs:
  - ${{ if eq(parameters.language, 'flutter') }}:
    - template: jobs/flutter-jobs.yml