jobs:
  - job: "build_nodejs"
    displayName: "Build nodejs"
    pool:
      name: "Azure Pipelines"
      vmImage: "ubuntu-22.04"
    variables:
      versionNode: $[coalesce(variables.nodeVersion, '18.20.7')]

    steps:
      - script: |
          ls 
        displayName: "📖 Read Microservice Versions"