jobs:

  - ${{ if not(startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/')) }}:
    - deployment: WaitForApproval
      displayName: 'Esperar aprobación'
      pool:
        name: Azure Pipelines
        vmImage: 'ubuntu-22.04'
      ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/develop') }}:
        environment: 'Build develop'
      ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/staging') }}:
        environment: 'Build Development Staging' 
      ${{ if and(or(in(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')), eq(parameters.red, 'antioquia')) }}:
        environment: 'Build master antioquia'
      ${{ if and(or(in(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')), eq(parameters.red, 'cap')) }}:
        environment: 'Build master cap'
      ${{ if and(or(in(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')), eq(parameters.red, 'tolima')) }}:
        environment: 'Build master tolima'
      ${{ if and(or(in(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')), eq(parameters.red, 'huila')) }}:
        environment: 'Build master huila'
      ${{ if and(or(in(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')), eq(parameters.red, 'cauca')) }}:
        environment: 'Build master cauca'
      strategy:
        runOnce:
          deploy:
            steps:
              - script: echo "Aprobación recibida. Iniciando despliegues..."

  - job: 'build_flutter'
    displayName: 'Build flutter'
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'

    variables:
      versionSpecification: $[coalesce(variables.flutterVersion, '3.7.x')]
      flutterCacheKey: $(Build.SourceVersion)
      
    steps:
      - task: DownloadBuildArtifacts@0
        inputs:
          artifactName: 'versions'
          downloadPath: '$(Pipeline.Workspace)/downloaded-versions'
        displayName: '📥 Descargar artifact de versiones'
        
      - script: |
            ls -R $(Pipeline.Workspace)/downloaded-versions/versions/version-apk
            VERSION_FILE="$(Pipeline.Workspace)/downloaded-versions/versions/version-apk/version-apk.json"
            if [[ -f "$VERSION_FILE" ]]; then
              VERSION=$(jq -r '.version' $VERSION_FILE)
              BASE_VERSION=$(echo "$VERSION" | cut -d'-' -f1)
              echo "🚀 Versión detectada para apk: $BASE_VERSION"
              echo "##vso[task.setvariable variable=msVersion]$BASE_VERSION"
            else
              echo "❌ No se encontró archivo de versión para apk"
              exit 1
            fi
        displayName: '📚 Leer versiones de microservicios'

      - task: JavaToolInstaller@0
        inputs:
          versionSpec: '17'
          jdkArchitectureOption: 'x64'
          jdkSourceOption: 'PreInstalled'
      
      - script: |
          echo "version a actualizar: $(msVersion)"
          sed -i "s/^version: .*/version: $(msVersion)/" pubspec.yaml
          cat pubspec.yaml
        displayName: 'change version file pubspect'
      
      - script: |
          echo "version a actualizar: $(msVersion)"
          jq --arg version "$NEW_VERSION" '.VERSION = $version' env.json > tmp.json && mv tmp.json env.json
          cat env.json
        displayName: 'change version file env.json'

      - task: FlutterInstall@0
        inputs:
          mode: 'auto'
          channel: 'stable'
          version: 'custom'
          customVersion: $(versionSpecification)

      - script: |
          flutter --version
          flutter pub get
        displayName: 'Flutter Pub Get'

      - script: |
          echo "Verificando carpeta assets"
          if [ -d "assets" ] && [ "$(ls -A assets)" ]; then
            echo "✅ Assets encontrados"
          else
            echo "❌ Error: No se encontraron assets"
            exit 99
          fi
        displayName: 'Verificar assets'

      - script: |
          flutter --no-color build apk --no-tree-shake-icons --profile
        displayName: 'Flutter Build APK (profile)'

      - script: |
          echo "Verificando Build Appk"
          if [ -d "build/app/outputs/flutter-apk" ] && [ "$(ls -A build/app/outputs/flutter-apk)" ]; then
            echo "✅ Apk encontrado"
          else
            echo "❌ Error: Apk no encontrado"
            exit 99
          fi
        displayName: 'Verificar Apk'

      - script: |
          echo "Renombrando APK..."
          CURRENT_BRANCH=$(echo $(Build.SourceBranch))
          case "$CURRENT_BRANCH" in
            "refs/heads/develop")
              CURRENT_BRANCH_PR="dev"
              ;;
            "refs/heads/staging")
              CURRENT_BRANCH_PR="qa"
              ;;
            "refs/heads/master")
              case "$RED" in
                "antioquia") CURRENT_BRANCH_PR="prd-gana" ;;
                "cap")       CURRENT_BRANCH_PR="cap" ;;
                "tolima")    CURRENT_BRANCH_PR="prd-seapto" ;;
                "huila")     CURRENT_BRANCH_PR="prd-suchance" ;;
                "cauca")     CURRENT_BRANCH_PR="prd-acertemos" ;;
                *)           echo "⚠️ Región $RED no reconocida para master"; exit 1 ;;
              esac
              ;;
            *)
              echo "⚠️ Rama $CURRENT_BRANCH no reconocida"; exit 1
              ;;
          esac

          mv build/app/outputs/flutter-apk/app-profile.apk build/app/outputs/flutter-apk/invictus-$CURRENT_BRANCH_PR-v$(msVersion).apk
        displayName: 'change name file Apk'
        condition: not(startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/'))
        env:
          RED: ${{ parameters.red }}

      - script: |
          CURRENT_BRANCH=$(echo $(Build.SourceBranch))
          case "$CURRENT_BRANCH" in
            "refs/heads/develop")
              CURRENT_BRANCH_PR="dev"
              ;;
            "refs/heads/staging")
              CURRENT_BRANCH_PR="qa"
              ;;
            "refs/heads/master")
              case "$RED" in
                "antioquia") CURRENT_BRANCH_PR="prd-gana" ;;
                "cap")       CURRENT_BRANCH_PR="cap" ;;
                "tolima")    CURRENT_BRANCH_PR="prd-seapto" ;;
                "huila")     CURRENT_BRANCH_PR="prd-suchance" ;;
                "cauca")     CURRENT_BRANCH_PR="prd-acertemos" ;;
                *)           echo "⚠️ Región $RED no reconocida para master"; exit 1 ;;
              esac
              ;;
            *)
              echo "⚠️ Rama $CURRENT_BRANCH no reconocida"; exit 1
              ;;
          esac
          
          aws s3 cp ./build/app/outputs/flutter-apk s3://repository-native/app-seller/android/pruebas-$CURRENT_BRANCH_PR --recursive
        displayName: 'Copy Apk Bucket S3'
        condition: not(startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/'))
        env:
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
