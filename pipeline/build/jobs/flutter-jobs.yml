jobs:
  - job: 'build_flutter'
    displayName: 'Build flutter'    
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'

    steps:
      - script: |
          echo "compila flutter"
          ls -l
        displayName: "Build flutter"
