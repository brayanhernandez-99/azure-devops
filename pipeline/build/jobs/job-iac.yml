jobs:
  # - ${{ if not(startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/')) }}:
  #     - deployment: WaitForApproval
  #       displayName: "Esperar aprobación"
  #       pool:
  #         vmImage: "ubuntu-22.04"
  #         environment: ${{ variables.environmentName }}
  #       variables:
  #         ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/develop') }}:
  #           environmentName: "Plan develop"
  #         ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/staging') }}:
  #           environmentName: "Plan staging"
  #         ${{ if or(eq(variables['Build.SourceBranchName'], 'master'), startsWith(variables['Build.SourceBranch'], 'refs/tags/')) }}:
  #           environmentName: "Plan master ${{ parameters.red }}"
  #       strategy:
  #         runOnce:
  #           deploy:
  #             steps:
  #               - script: echo "Aprobación recibida. Iniciando despliegues..."

  - ${{ if eq(variables['Build.SourceBranchName'], 'tolima') }}:
    - job: "build_iac_sebas_tolima"
      displayName: "Build Infrastructure"
      pool:
        name: "Azure Pipelines"
        vmImage: "ubuntu-22.04"

      variables:
        versionTerraform: $[coalesce(variables.terraformVersion, '1.12.0')]
        versionTerragrunt: $[coalesce(variables.terragruntVersion, '0.84.1')]

      steps:
        - script: |
            echo -e "\n📋 Variables disponibles:"
            printenv | sort | grep -i "aws_" || true

            echo -e "\n📋 Parametros disponibles:"
            printenv | sort | grep -i "parameter_" || true
          displayName: "📋 Environment variables"

        - script: |
            echo "📥 Descargando Terragrunt..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terragrunt/terragrunt-$(versionTerragrunt)" "terragrunt"

            if [ ! -s terragrunt ]; then
              echo "❌ Error: No se pudo descargar terragrunt: $(versionTerragrunt)"
              exit 99
            fi

            chmod +x terragrunt
            sudo mv terragrunt /usr/local/bin/terragrunt

            echo -e "\n🚀 Terragrunt instalado correctamente:"
            terragrunt --version
          displayName: "🛠️ Instalar Terragrunt"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - script: |
            echo "📥 Descargando Terraform..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terraform/terraform-$(versionTerraform)" "terraform"

            if [ ! -s terraform ]; then
              echo "❌ Error: No se pudo descargar terraform: $(versionTerraform)"
              exit 99
            fi

            chmod +x terraform
            sudo mv terraform /usr/local/bin/terraform

            echo -e "\n🚀 Terraform instalado correctamente:"
            terraform --version
          displayName: "🛠️ Instalar terraform"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - task: replacetokens@5
          inputs:
            targetFiles: |
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/**/terragrunt.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/root.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/backend.tf
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/provider.tf
            encoding: "auto"
            writeBOM: true
            actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
            keepToken: false
            tokenPrefix: "#{"
            tokenSuffix: "}#"
          displayName: "⚙️ Replace Tokens"

        - script: |
            set -e

            
            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/initial-infrastructure/aws-service-apigateway-metabase
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/initial-infrastructure/aws-service-apigateway-domain-names-metabase
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/initial-infrastructure/aws-service-iam-profile/ecs-task-execution-role
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/metabase/aws-service-ecs
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/initial-infrastructure/aws-service-secret-manager-aes
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/initial-infrastructure/aws-service-ssm-parameter
            # # terragrunt plan --all --non-interactive --no-color
            # # terragrunt apply --all --non-interactive --no-color

            # # echo "*********************redis***********************"

            # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/transaction-notifier
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color

            # echo "*********************remittances***********************"

            # cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima/wiretransfer
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color

          displayName: "✔️ Plan"
          workingDirectory: $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/tolima
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

  - ${{ if eq(variables['Build.SourceBranchName'], 'huila') }}:
    - job: "build_iac_sebas_huila"
      displayName: "Build Infrastructure"
      pool:
        name: "Azure Pipelines"
        vmImage: "ubuntu-22.04"

      variables:
        versionTerraform: $[coalesce(variables.terraformVersion, '1.12.0')]
        versionTerragrunt: $[coalesce(variables.terragruntVersion, '0.84.1')]

      steps:
        - script: |
            echo -e "\n📋 Variables disponibles:"
            printenv | sort | grep -i "aws_" || true

            echo -e "\n📋 Parametros disponibles:"
            printenv | sort | grep -i "parameter_" || true
          displayName: "📋 Environment variables"

        - script: |
            echo "📥 Descargando Terragrunt..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terragrunt/terragrunt-$(versionTerragrunt)" "terragrunt"

            if [ ! -s terragrunt ]; then
              echo "❌ Error: No se pudo descargar terragrunt: $(versionTerragrunt)"
              exit 99
            fi

            chmod +x terragrunt
            sudo mv terragrunt /usr/local/bin/terragrunt

            echo -e "\n🚀 Terragrunt instalado correctamente:"
            terragrunt --version
          displayName: "🛠️ Instalar Terragrunt"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - script: |
            echo "📥 Descargando Terraform..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terraform/terraform-$(versionTerraform)" "terraform"

            if [ ! -s terraform ]; then
              echo "❌ Error: No se pudo descargar terraform: $(versionTerraform)"
              exit 99
            fi

            chmod +x terraform
            sudo mv terraform /usr/local/bin/terraform

            echo -e "\n🚀 Terraform instalado correctamente:"
            terraform --version
          displayName: "🛠️ Instalar terraform"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - task: replacetokens@5
          inputs:
            targetFiles: |
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/**/terragrunt.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/root.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/backend.tf
              $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/provider.tf
            encoding: "auto"
            writeBOM: true
            actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
            keepToken: false
            tokenPrefix: "#{"
            tokenSuffix: "}#"
          displayName: "⚙️ Replace Tokens"

        - script: |
            set -e


            cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/astro
            terragrunt plan --all --non-interactive --no-color
            terragrunt apply --all --non-interactive --no-color

            cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/awards
            terragrunt plan --all --non-interactive --no-color
            terragrunt apply --all --non-interactive --no-color

            cd $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila/bff-administration
            terragrunt plan --all --non-interactive --no-color
            terragrunt apply --all --non-interactive --no-color

            

          displayName: "✔️ Plan"
          workingDirectory: $(Build.SourcesDirectory)/iac-terragrunt/project/invictus/huila
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

  - ${{ if or(eq(variables['Build.SourceBranch'], 'refs/heads/feature/develop'), eq(variables['Build.SourceBranch'], 'refs/heads/feature/staging')) }}:
    - job: "build_iac_sebas"
      displayName: "Build Infrastructure"
      pool:
        name: "Azure Pipelines"
        vmImage: "ubuntu-22.04"

      variables:
        versionTerraform: $[coalesce(variables.terraformVersion, '1.12.0')]
        versionTerragrunt: $[coalesce(variables.terragruntVersion, '0.84.1')]

      steps:
        - script: |
            echo -e "\n📋 Variables disponibles:"
            printenv | sort | grep -i "aws_" || true

            echo -e "\n📋 Parametros disponibles:"
            printenv | sort | grep -i "parameter_" || true
          displayName: "📋 Environment variables"

        - script: |
            echo "📥 Descargando Terragrunt..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terragrunt/terragrunt-$(versionTerragrunt)" "terragrunt"

            if [ ! -s terragrunt ]; then
              echo "❌ Error: No se pudo descargar terragrunt: $(versionTerragrunt)"
              exit 99
            fi

            chmod +x terragrunt
            sudo mv terragrunt /usr/local/bin/terragrunt

            echo -e "\n🚀 Terragrunt instalado correctamente:"
            terragrunt --version
          displayName: "🛠️ Instalar Terragrunt"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - script: |
            echo "📥 Descargando Terraform..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terraform/terraform-$(versionTerraform)" "terraform"

            if [ ! -s terraform ]; then
              echo "❌ Error: No se pudo descargar terraform: $(versionTerraform)"
              exit 99
            fi

            chmod +x terraform
            sudo mv terraform /usr/local/bin/terraform

            echo -e "\n🚀 Terraform instalado correctamente:"
            terraform --version
          displayName: "🛠️ Instalar terraform"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - task: replacetokens@5
          inputs:
            targetFiles: |
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/**/terragrunt.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/root.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/backend.tf
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/provider.tf
            encoding: "auto"
            writeBOM: true
            actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
            keepToken: false
            tokenPrefix: "#{"
            tokenSuffix: "}#"
          displayName: "⚙️ Replace Tokens"

        - script: |
            set -e


            cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-ssm-parameter
            terragrunt plan --all --non-interactive --no-color
            terragrunt apply --all --non-interactive --no-color



            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-apigateway-metabase
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-apigateway-domain-names-metabase
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-iam-profile/ecs-task-execution-role
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/awards/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-administration/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-integration/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-sellers/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/biometrics/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/collected/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/conciliation/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/dynamic-storage/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/external-wager/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/generic-services/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/hierarchies/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/legacy/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries-games/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/metabase/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/millonario/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/money-control/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/notifications/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/online-games/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/papelery/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payment-voucher/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payments/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/products/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/promotional/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/raffles/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/recharges/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/redis/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/remittances/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/reports/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/security/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/sellers/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/shopping-cart/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/siga/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction-notifier/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/wiretransfer/aws-service-ecs
            # terragrunt plan --all --non-interactive
            # terragrunt apply --all --non-interactive

          displayName: "✔️ Plan"
          workingDirectory: $(Build.SourcesDirectory)/iac-terragrunt/invictus/
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)


  - ${{ if or(eq(variables['Build.SourceBranch'], 'refs/heads/feature/cap'), eq(variables['Build.SourceBranch'], 'refs/heads/feature/santander'), eq(variables['Build.SourceBranch'], 'refs/heads/feature/gana'), eq(variables['Build.SourceBranch'], 'refs/heads/develop')) }}:
    - job: "build_iac_brayan"
      displayName: "Build Infrastructure"
      pool:
        name: "Azure Pipelines"
        vmImage: "ubuntu-22.04"

      variables:
        versionTerraform: $[coalesce(variables.terraformVersion, '1.12.0')]
        versionTerragrunt: $[coalesce(variables.terragruntVersion, '0.84.1')]

      steps:
        - script: |
            echo -e "\n📋 Variables disponibles:"
            printenv | sort | grep -i "aws_" || true

            echo -e "\n📋 Parametros disponibles:"
            printenv | sort | grep -i "parameter_" || true
          displayName: "📋 Environment variables"

        - script: |
            echo "📥 Descargando Terragrunt..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terragrunt/terragrunt-$(versionTerragrunt)" "terragrunt"

            if [ ! -s terragrunt ]; then
              echo "❌ Error: No se pudo descargar terragrunt: $(versionTerragrunt)"
              exit 99
            fi

            chmod +x terragrunt
            sudo mv terragrunt /usr/local/bin/terragrunt

            echo -e "\n🚀 Terragrunt instalado correctamente:"
            terragrunt --version
          displayName: "🛠️ Instalar Terragrunt"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - script: |
            echo "📥 Descargando Terraform..."
            aws s3 cp "s3://infrastructure-as-code-invictus/terraform/terraform-$(versionTerraform)" "terraform"

            if [ ! -s terraform ]; then
              echo "❌ Error: No se pudo descargar terraform: $(versionTerraform)"
              exit 99
            fi

            chmod +x terraform
            sudo mv terraform /usr/local/bin/terraform

            echo -e "\n🚀 Terraform instalado correctamente:"
            terraform --version
          displayName: "🛠️ Instalar terraform"
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

        - task: replacetokens@5
          inputs:
            targetFiles: |
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/**/terragrunt.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/root.hcl
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/backend.tf
              $(Build.SourcesDirectory)/iac-terragrunt/invictus/provider.tf
            encoding: "auto"
            writeBOM: true
            actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
            keepToken: false
            tokenPrefix: "#{"
            tokenSuffix: "}#"
          displayName: "⚙️ Replace Tokens"

        - script: |
            set -e

          
            cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/awards
            terragrunt plan --all --non-interactive --no-color
            terragrunt apply --all --non-interactive --no-color

            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-secret-manager
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color
            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-secret-manager-betplay
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color
            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-sqs
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color
            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-s3
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color
            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-ecs
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color
            # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-ssm-parameter
            # terragrunt plan --all --non-interactive --no-color
            # terragrunt apply --all --non-interactive --no-color



          displayName: "✔️ Plan"
          workingDirectory: $(Build.SourcesDirectory)/iac-terragrunt/invictus/
          env:
            AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
            AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
            AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
