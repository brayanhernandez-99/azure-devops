jobs:
  - job: 'build_nodejs'
    displayName: 'Build nodejs'    
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'
    variables:
    authVer: $[ dependencies.detectVersions.outputs['setVersions.auth_version'] ]

    steps:
      
      - script: |
          echo "build nodejs"

      - script: |
          echo "segundo task"

