parameters:
  dependsOn: []
  changedMs: []

jobs:
  - job: 'build_nodejs'
    dependsOn: ${{ parameters.dependsOn }}
    displayName: 'Build nodejs'    
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'
    variables:
      changedMs: $[ dependencies.semantic_version.outputs['DetectChangedMS.changedMs'] ]
    # strategy:
    #   matrix:
    #     ${{ each ms in parameters.changedMs }}:
    #       ${{ ms }}:
    #         msName: ${{ ms }}

    steps:
      
      - script: |
          echo $(changedMs)

      - script: |
          echo "segundo task"

