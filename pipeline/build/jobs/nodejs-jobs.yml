jobs:
  - job: 'build_nodejs'
    dependsOn: semantic_version
    displayName: 'Build nodejs'    
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'

    strategy:
      matrix:
        ${{ each ms in parameters.apps }}:
          ${{ ms }}:
            msName: ${{ ms }}

    steps:
      - script: |
          echo "Compilando microservicio: $(msName)"
        displayName: "Build $(msName)"

      # - script: |
      #     echo $(changedMs)
      #     echo ${{parameters.changedMs }}

      # - script: |
      #     echo "segundo task"

