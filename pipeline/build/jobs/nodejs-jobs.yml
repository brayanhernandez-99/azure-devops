jobs:
  - job: 'build_nodejs'
    displayName: 'Build nodejs'    
    pool:
        name: 'Azure Pipelines'
        vmImage: latest

    steps:
      
      - script: |
          echo "build nodejs"

