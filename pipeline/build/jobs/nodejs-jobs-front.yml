jobs:
  - job: 'build_nodejs'
    displayName: 'Build nodejs'    
    pool:
        name: 'Azure Pipelines'
        vmImage: 'ubuntu-22.04'
    variables:
      versionSpecification: $[coalesce(variables.nodejsVersion, '18.x')]

    # strategy:
    #   matrix:
    #     ${{ each ms in parameters.apps }}:
    #       ${{ ms }}:
    #         msName: ${{ ms }}

    steps:
      - script: | 
          ls -l
        displayName: "Build"

      - task: NodeTool@0
        displayName: 'Use Nodejs $(versionSpecification)'
        inputs:
          versionSource: 'spec'
          versionSpec: '$(versionSpecification)'
        
      - script: |
          npm install -g pnpm
        displayName: 'Instalar pnpm'

      - task: Bash@3
        name: AuthCodeArtifact
        inputs:
          targetType: 'inline'
          script: |
            export CODEARTIFACT_AUTH_TOKEN=$(aws codeartifact get-authorization-token \
              --domain matrixtech-npm-repository \
              --domain-owner $ACCOUNT_ID_DEPLOY \
              --region $AWS_DEFAULT_REGION \
              --query authorizationToken \
              --output text )
            npm config set //matrixtech-npm-repository-$ACCOUNT_ID_DEPLOY.d.codeartifact.$AWS_DEFAULT_REGION.amazonaws.com/npm/matrixtech-npm-repository/:_authToken=$CODEARTIFACT_AUTH_TOKEN
            npm config set registry https://matrixtech-npm-repository-861262569826.d.codeartifact.$AWS_DEFAULT_REGION.amazonaws.com/npm/matrixtech-npm-repository/
            aws sts get-caller-identity

            echo "//matrixtech-npm-repository-$ACCOUNT_ID_DEPLOY.d.codeartifact.$AWS_DEFAULT_REGION.amazonaws.com/npm/matrixtech-npm-repository/:_authToken=$CODEARTIFACT_AUTH_TOKEN" > apps/awards/.npmrc
            echo "registry=https://matrixtech-npm-repository-$ACCOUNT_ID_DEPLOY.d.codeartifact.$AWS_DEFAULT_REGION.amazonaws.com/npm/matrixtech-npm-repository/" >> apps/awards/.npmrc
        env:
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)

      - script: |
          echo "🛠 Generando .env.local para administration-vue2"
          printenv | grep '^ADMIN_' | sed 's/^ADMIN_//' > app-legacy/app-administration-vue2/.env.local
          cat app-legacy/app-administration-vue2/.env.local

          echo " "

          echo "🛠 Generando .env.local para sellers-vue2"
          printenv | grep '^SELLERS_' | sed 's/^SELLERS_//' > app-legacy/app-sellers-vue2/.env.local
          cat app-legacy/app-sellers-vue2/.env.local

          ls -la app-legacy/app-administration-vue2
          ls -la app-legacy/app-sellers-vue2

        displayName: 'Generación de .env admin y seller'


      - script: |
          CURRENT_BRANCH=$(echo $(Build.SourceBranch))
          if [[ "$CURRENT_BRANCH" == refs/heads/feature/* ]]; then
            CURRENT_BRANCH_PR=dev
          fi
          if [[ "$CURRENT_BRANCH" == refs/heads/develop ]]; then
            CURRENT_BRANCH_PR=dev
          fi
          if [[ "$CURRENT_BRANCH" == refs/heads/staging ]]; then
            CURRENT_BRANCH_PR=qa
          fi
          # if [[ "$CURRENT_BRANCH" == refs/heads/feature/* ]]; then
          #   CURRENT_BRANCH_PR=refs/heads/feature
          # fi
          # if [[ "$CURRENT_BRANCH" == refs/heads/feature/* ]]; then
          #   CURRENT_BRANCH_PR=refs/heads/feature
          # fi
          # if [[ "$CURRENT_BRANCH" == refs/heads/feature/* ]]; then
          #   CURRENT_BRANCH_PR=refs/heads/feature
          # fi
          # if [[ "$CURRENT_BRANCH" == refs/heads/feature/* ]]; then
          #   CURRENT_BRANCH_PR=refs/heads/feature
          # fi

          echo "🛠 Generando .env.local para invictus"
          echo $CURRENT_BRANCH_PR
          printenv | grep '^VITE_' > apps/invictus/.env.$CURRENT_BRANCH_PR
          # cat apps/invictus/.env.$CURRENT_BRANCH_PR

          cp -R apps/invictus/.env.$CURRENT_BRANCH_PR apps/invictus/.env
          cat apps/invictus/.env
        displayName: 'Generación de .env invictus'

      - script: |
          echo -e "VITE_APP_VERSION=\$npm_package_version\nVITE_BASE_URL=" > apps/awards/.env
          cat apps/awards/.env
        displayName: 'cp env'

      - script: |
          echo "Estás en la carpeta: $(System.DefaultWorkingDirectory)/apps/awards"
          cd apps/awards
          pwd 
          ls -l
          # npm install --ignore-scripts
          # pnpm i --ignore-scripts
          pnpm i 
          echo "Dependencias instaladas:"
          npm ls --depth=0 || true
        displayName: 'Instalar dependencias del microservicio'

      - script: |
          pnpm add -D dotenv-cli
          pnpm build --filter=...awards
          ls -la
          ls -la apps/awards/dist
        displayName: 'compilar dependencias del microservicio'

      
      - task: JavaToolInstaller@0
        inputs:
          versionSpec: '17'
          jdkArchitectureOption: 'x64'
          jdkSourceOption: 'PreInstalled'

      - task: SonarQubePrepare@7
        inputs:
          SonarQube: 'SonarQubeConection'
          scannerMode: 'CLI'
          configMode: 'manual'
          cliProjectKey: 'pruebas-microfronted-awards'
          cliProjectName: 'pruebas-microfronted-awards'
          cliSources: '$(System.DefaultWorkingDirectory)/apps/awards'
          extraProperties: |
            sonar.javascript.lcov.reportPaths=apps/awards/coverage/lcov.info
            sonar.exclusions=**/*.java,**/test/**,**/android/**,**/ios/**,**/coverage/**,**/.nyc_output/**,**/reports/**,**/node_modules/**,**/migrations/**,**/config/**

      - script: |
          variable=$(echo $SONARQUBE_SCANNER_PARAMS | sed -E 's#"sonar\.branch\.name":"[^"]+",##g')
          echo $variable
          echo "##vso[task.setvariable variable=SONARQUBE_SCANNER_PARAMS]$variable"
        displayName: 'set-variable'

      - script: |
          ls -l
          cd apps/awards
          npm run test 
          npm run coverage
        displayName: 'Test MS awards'
        continueOnError: true

      - script: |
          cd apps/awards
          npm run coverage
          tree
          ls -l apps/awards/coverage
        displayName: 'Coverage MS astro'
        continueOnError: true

      - task: SonarQubeAnalyze@7
        displayName: 'Sonar Analysis'

      - task: SonarQubePublish@7
        displayName: 'Publish Result Static Code Analysis'
        inputs:
          pollingTimeoutSec: 400
      
      - task: sonar-buildbreaker@8
        displayName: 'Quality Gate'
        inputs:
          SonarQube: 'SonarQubeConection'