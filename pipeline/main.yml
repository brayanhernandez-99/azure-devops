parameters:
  - name: release
    type: string
    values:
      - ecs-redes

  - name: red
    type: object
    default:
      - antioquia
      - cap
      - tolima
      - huila
      - cauca
      - boyaca
      - santander

  - name: apps
    type: object
    default:
      microservice-astro: "3.5.5"
      microservice-awards: "3.5.1"
      microservice-biometrics: "1.1.3"
      microservice-collected: "3.5.2"
      microservice-conciliation: "1.4.3"
      microservice-dynamic-storage: "1.6.3"
      microservice-external-wager: "2.4.7"
      microservice-generic-services: "2.4.3"
      microservice-hierarchies: "3.5.4"
      microservice-legacy: "3.1.2"
      microservice-lotteries: "2.4.3"
      microservice-lotteries-games: "4.13.7"
      microservice-millonario: "3.4.4"
      microservice-money-control: "1.2.2"
      microservice-online-games: "3.4.3"
      microservice-papelery: "3.3.7"
      microservice-payment-voucher: "3.1.8"
      microservice-payments: "3.4.1"
      microservice-products: "3.5.1"
      microservice-promotional: "1.1.2"
      microservice-raffle: "3.5.1"
      microservice-recharges: "3.4.5"
      microservice-remittances: "2.2.4"
      microservice-sellers: "3.5.6"
      microservice-siga: "3.2.9"
      microservice-transaction: "4.5.5"
      microservice-security: "3.3.8"

resources:
  repositories:
    - repository: self
      name: $(System.TeamProject)/$(Build.Repository.Name)
      type: git
      ref: $(Build.SourceBranch)

stages:

 #DEPLOY REDES MOMENTANEMENTE 
  - ${{ if and(eq(variables['Build.SourceBranch'], 'refs/heads/master'), eq(parameters.release, 'ecs-redes')) }}:
      - template: build/semantic-version-integration.yml
        parameters:
          release: ${{ parameters.release }}
          apps: ${{ parameters.apps }}

  # # Deploy integración si NO es Flutter, No es Electron y es tag, develop, staging
  - ${{ if and(eq(variables['Build.SourceBranch'], 'refs/heads/master'), eq(parameters.release, 'ecs-redes')) }}:
      - ${{ each region in parameters.red }}:
          - template: release/deploy-integration.yml
            parameters:
              release: ${{ parameters.release }}
              apps: ${{ parameters.apps }}
              red: ${{ region }}
