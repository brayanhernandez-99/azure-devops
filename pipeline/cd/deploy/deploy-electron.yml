jobs:
  - job: deploy_electron
    displayName: "🚀 deploy electron"
    pool:
      name: "Azure Pipelines"
      VmImage: ubuntu-22.04

    variables:
      - template: ../../variables/groups-transversal-variables-electron.yml
        parameters:
          region: ${{ parameters.region }}
          environment_staging: ${{ parameters.environment_staging }}

    steps:
      - task: DownloadBuildArtifacts@0
        inputs:
          artifactName: "release"
          downloadPath: "$(Pipeline.Workspace)/release"
        displayName: "📥 Download Electron Artifacts"
        condition: succeeded()

      - task: DownloadBuildArtifacts@0
        inputs:
          artifactName: "versions"
          downloadPath: "$(Pipeline.Workspace)/versions-downloaded"
        displayName: "📥 Download Version"
        condition: succeeded()

      - script: |
          VERSION_FILE="$(Pipeline.Workspace)/versions-downloaded/versions/electron/version.json"
          if [[ -f "$VERSION_FILE" ]]; then
            VERSION=$(jq -r '.version' $VERSION_FILE)
            echo -e "\n🚀 Versión detectada para $(Build.Repository.Name): $VERSION"
            echo "##vso[task.setvariable variable=version]$VERSION"
          else
            echo -e "\n##vso[task.logissue type=error]⚠️ No se encontro archivo de versión para $(Build.Repository.Name)"
            exit 99
          fi
        displayName: "📚 Read Version"
        condition: succeeded()

      - script: |
          CURRENT_BRANCH="$(Build.SourceBranch)"
          environment="unknown"

          case "$CURRENT_BRANCH" in 
            refs/heads/develop | refs/heads/feature/* | refs/heads/hotfix/*)
              environment="develop"
            ;;
            refs/heads/staging)
              if [[ "$environment_staging" == "staging" ]]; then
                environment="staging"
              elif [[ "$environment_staging" == "staging_nacional" ]]; then
                environment="staging_nacional"
              fi
            ;;
            refs/heads/master)
              environment = "$region"
            ;;
          esac

          if [[ "$environment" = "unknown" ]]; then
            echo -e "\n##vso[task.logissue type=error]⚠️ Environment [$environment] no reconocido"
            exit 99
          fi

          echo "##vso[task.setvariable variable=environment]$environment"
          echo -e "\n✅ Environment asignado exitosamente"
          echo "Environment: $environment"


          tree
          
        displayName: "⚙️ Generate .env"
        condition: succeeded()
        env:
          region: ${{ parameters.region }}
          environments: ${{ parameters.environments }}

      # - script: |
      #     set -e
      #     echo -e "\n📦 Cargando al Bucket S3..."

      #     BASE_PATH="$(Pipeline.Workspace)/release/electron/linux/$(environment)"
      #     echo -e "\n📂 Archivos a cargar: $BASE_PATH"
      #     ls -l "$BASE_PATH"
      #     echo ""

      #     aws s3 cp "$BASE_PATH" "s3://mf-download-171158266043/app/linux/$(environment)" --recursive --acl public-read

      #     echo -e "\n✅ Archivos subidos exitosamente."
      #     echo "🔗 APP: InvictusUX-$(version).AppImage"
      #     echo -e "\n##[command]🔗 URL: https://mf-download-171158266043.s3.us-east-1.amazonaws.com/app/linux/$(environment)/InvictusUX-$(version).AppImage"
      #   displayName: "🐧 Upload Bucket S3 Linux"
      #   condition: succeeded()
      #   env:
      #     AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
      #     AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
      #     AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      # - script: |
      #     set -e
      #     echo -e "\n📦 Cargando al Bucket S3..."

      #     BASE_PATH="$(Pipeline.Workspace)/release/electron/windows/$(environment)"
      #     echo -e "\n📂 Archivos a cargar: $BASE_PATH"
      #     ls -l "$BASE_PATH"
      #     echo ""

      #     aws s3 cp "$BASE_PATH" "s3://mf-download-171158266043/app/windows/$(environment)" --recursive --acl public-read

      #     echo -e "\n✅ Archivos subidos exitosamente."
      #     echo "🔗 APP: InvictusUX Setup $(version).exe"
      #     echo -e "\n##[command] URL: https://mf-download-171158266043.s3.us-east-1.amazonaws.com/app/windows/$(environment)/InvictusUX+Setup+$(version).exe"
      #   displayName: "🖥️ Upload Bucket S3 Windows"
      #   condition: succeeded()
      #   env:
      #     AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
      #     AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
      #     AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      # - script: |
      #     set -e
      #     echo -e "\n📦 Send Message SQS..."
      #     echo "📦 Account: $(ACCOUNT_ID_INVICTUS)"
      #     echo "📦 Version: $(version)"

      #     aws sqs send-message \
      #       --queue-url https://sqs.us-east-1.amazonaws.com/$(ACCOUNT_ID_INVICTUS)/DynamicStorage \
      #       --message-body '{
      #         "model": {
      #           "parameterName": "VERSION",
      #           "parameterDescription": "Parámetro que indica la versión actual del sistema.",
      #           "parameterOrigin": "GLOBAL_PARAMETER",
      #           "parameterValue": "$(version)"
      #         },
      #         "event": "setParameter"
      #       }' \
      #       --delay-seconds 0
      #   displayName: "📩 Send Message SQS"
      #   env:
      #     AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
      #     AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
      #     AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)

      # - script: |
      #     DEPLOY_TIME=$(date -d "$BUILD_FINISHTIME -5 hours" "+%Y-%m-%d %H:%M UTC")
      #     BRANCH_NAME_MULTI=$environments
      #     BRANCH_NAME="$BUILD_SOURCEBRANCHNAME"

      #     if [[ "$BRANCH_NAME" == "master" ]]; then
      #       BRANCH_DEPLOY="${red:-red}"

      #     elif [[ "$BRANCH_NAME" == "develop" && "$BRANCH_NAME_MULTI" == "develop" ]]; then
      #       BRANCH_DEPLOY="develop"

      #     elif [[ "$BRANCH_NAME" == "staging" && "$BRANCH_NAME_MULTI" == "staging" ]]; then
      #       BRANCH_DEPLOY="staging"

      #     else
      #       BRANCH_DEPLOY="$BRANCH_NAME_MULTI"
      #     fi

      #     payload=$(cat <<EOF
      #     {
      #       "@type": "MessageCard",
      #       "@context": "http://schema.org/extensions",
      #       "summary": "Deployment successful",
      #       "themeColor": "00FF00",
      #       "title": "✅ Deployment exitoso $(Build.SourceBranchName)",
      #       "sections": [
      #         {
      #           "facts": [
      #             { "name": "Pipeline", "value": "$(Build.DefinitionName)" },
      #             { "name": "Branch", "value": "$BRANCH_DEPLOY" },
      #             { "name": "Build", "value": "$(Build.BuildNumber)" },
      #             { "name": "Merged by", "value": "$(Build.RequestedFor)" },
      #             { "name": "Deploy time", "value": "$DEPLOY_TIME" }
      #           ],
      #           "markdown": true
      #         }
      #       ]
      #     }
      #     EOF
      #     )

      #     curl -H "Content-Type: application/json" \
      #          -d "$payload" \
      #          $(URL_WEBHOOK_TEAMS)
      #   displayName: "📢 Post message to Teams"
      #   env:
      #     red: ${{ parameters.region }}
      #     environments: ${{ parameters.environments }}
