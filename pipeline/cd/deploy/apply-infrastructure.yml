jobs:
  - job: apply_infrastructure_${{ parameters.region }}
    displayName: "🚀 terragrunt apply"
    pool:
      name: "Azure Pipelines"
      VmImage: ubuntu-22.04

    variables:
      - template: ../../variables/groups-transversal-variables-iac.yml
        parameters:
          region: ${{ parameters.region }}

    steps:
      - script: |
          echo -e "\n📋 Variables disponibles:"
          printenv | sort | grep -i "aws_" || true

          echo -e "\n📋 Parametros disponibles:"
          printenv | sort | grep -i "parameter_" || true
        displayName: "📋 Environment variables"
        condition: succeeded()

      - script: |
          echo "📥 Descargando Terragrunt $(terragruntVersion)..."
          aws s3 cp "s3://infrastructure-as-code-invictus/terragrunt/terragrunt-$(terragruntVersion)" "terragrunt"

          if [ ! -s terragrunt ]; then
            echo "##vso[task.logissue type=error]⚠️ Error: No se pudo descargar terragrunt $(terragruntVersion)"
            exit 99
          fi

          chmod +x terragrunt
          sudo mv terragrunt /usr/local/bin/terragrunt

          echo -e "\n🚀 Terragrunt instalado correctamente:"
          terragrunt --version
        displayName: "🛠️ Instalando Terragrunt $(terragruntVersion)"
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      - script: |
          echo "📥 Descargando Terraform..."
          aws s3 cp "s3://infrastructure-as-code-invictus/terraform/terraform-$(terraformVersion)" "terraform"

          if [ ! -s terraform ]; then
            echo "##vso[task.logissue type=error]⚠️ Error: No se pudo descargar terraform $(terraformVersion)"
            exit 99
          fi

          chmod +x terraform
          sudo mv terraform /usr/local/bin/terraform

          echo -e "\n🚀 Terraform instalado correctamente:"
          terraform --version
        displayName: "🛠️ Instalando Terraform $(terraformVersion)"
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      - task: replacetokens@5
        inputs:
          targetFiles: |
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/**/terragrunt.hcl
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/root.hcl
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/backend.tf
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/provider.tf
          encoding: "auto"
          writeBOM: true
          actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
          keepToken: false
          tokenPrefix: "#{"
          tokenSuffix: "}#"
        displayName: "⚙️ Replace Tokens"
        condition: succeeded()

      - script: |
          set -e




          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/awards/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-administration/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-integration/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/bff-sellers/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/collected/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/biometrics/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/external-wager/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/legacy/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/conciliation/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/dynamic-storage/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/generic-services/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/hierarchies/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/metabase/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/millonario/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/money-control/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/notifier/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/notifications/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/online-games/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payments/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payment-voucher/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/products/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/promotional/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/raffles/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/raspa/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/recharges/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/redis/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/redis-cache/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/remittances/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/reports/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/scheduler/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/security/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/sellers/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/shopping-cart/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/siga/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/trino/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/wiretransfer/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction-astro/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction-lotteries-games/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction-notifier/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/transaction-recharges/
          terragrunt plan --non-interactive -no-color
          terragrunt apply --non-interactive -no-color




















          ####### SECRETS BN3, BN4, CHC FALTA EN AMBIENTES PRODUCTIVOS
          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries-games/aws-service-secret-manager-national-bn3-secret
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color
          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries-games/aws-service-secret-manager-national-bn4-secret
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color
          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries-games/aws-service-secret-manager-national-chc-secret
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color


          ##########
          # # # # ##Pruebas CrossAccountRole

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-iam-profile/deploy-CrossAccountRole
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color




          ##########
          # # # # #PERMISOS S3 
          #######

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/astro/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/awards/aws-service-s3
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/biometrics/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)
          # # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/collected/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/conciliation/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/dynamic-storage/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)
          # # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/external-wager/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/generic-services/aws-service-s3
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/lotteries-games/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)
          # # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/millonario/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/online-games/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payment-voucher/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)
          # # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payments/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/payments/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/products/aws-service-s3
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color

          # cd $(Build.SourcesDirectory)
          # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/products/aws-service-s3-products-icons
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/recharges/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/security/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # cd $(Build.SourcesDirectory)
          # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

          # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/sellers/aws-service-s3
          # terragrunt plan --all --non-interactive -no-color
          # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/shopping-cart/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-s3-lotteries-physical
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color

          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/remittances/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color





          ##########
          ####        SERVICIOS PENDIENTES DE DESPLEGAR EN PDN
          ###########

          # ################# Servicio de trino (LISTO EN DEV Y QA PENDIENTE EN LAS PDN)
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/trino
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color


          # #################### Eliminar redis  (LISTO EN DEV Y QA PENDIENTE EN LAS PDN)
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/redis
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt destroy --all --non-interactive -no-color


          # #################### Servicio de FTP Recaudos (LISTO EN DEV Y QA PENDIENTE EN LAS PDN)
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-ftp/aws-service-s3
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-ftp/aws-service-iam
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-ftp/aws-service-sg
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color
          # # cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/initial-infrastructure/aws-service-ftp/aws-service-ec2
          # # terragrunt plan --all --non-interactive -no-color
          # # terragrunt apply --all --non-interactive -no-color


          # # cd $(Build.SourcesDirectory)
          # # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +

        displayName: "🚀 Apply Infrastructure"
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
