jobs:
  - job: apply_infrastructure_${{ parameters.region }}
    displayName: "🚀 terragrunt apply"
    timeoutInMinutes: 120
    pool:
      name: "Azure Pipelines"
      VmImage: ubuntu-22.04

    variables:
      - template: ../../variables/groups-transversal-variables-iac.yml
        parameters:
          region: ${{ parameters.region }}
          environments: ${{ parameters.environments }}

    steps:
      - script: |
          echo -e "\n📋 Variables disponibles:"
          printenv | sort | grep -i "aws_" || true

          echo -e "\n📋 Parametros disponibles:"
          printenv | sort | grep -i "parameter_" || true
        displayName: "📋 Environment variables"
        condition: succeeded()

      - script: |
          echo "📥 Descargando Terragrunt 0.84.1..."
          curl -L "https://github.com/gruntwork-io/terragrunt/releases/download/v0.84.1/terragrunt_linux_amd64" -o terragrunt

          chmod +x terragrunt
          sudo mv terragrunt /usr/local/bin/terragrunt

          echo "🚀 Terragrunt instalado:"
          terragrunt --version
        displayName: "🛠️ Instalando Terragrunt 0.84.1"
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      - script: |
          echo "📥 Descargando Terraform 1.12.0..."
          curl -L "https://releases.hashicorp.com/terraform/1.12.0/terraform_1.12.0_linux_amd64.zip" -o terraform.zip

          unzip terraform.zip
          chmod +x terraform
          sudo mv terraform /usr/local/bin/terraform

          echo "🚀 Terraform instalado:"
          terraform --version
        displayName: "🛠️ Instalando Terraform 1.12.0"
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_DEPLOY)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_DEPLOY)

      - task: replacetokens@5
        inputs:
          targetFiles: |
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/**/terragrunt.hcl
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/root.hcl
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/backend.tf
            $(Build.SourcesDirectory)/iac-terragrunt/invictus/provider.tf
          encoding: "auto"
          writeBOM: true
          actionOnMissing: "warn" # 'fail', 'warn' o 'ignore'
          keepToken: false
          tokenPrefix: "#{"
          tokenSuffix: "}#"
        displayName: "⚙️ Replace Tokens"
        condition: succeeded()

      - script: |
          set -e
          

          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-app-administration-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-app-sellers-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-astro-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-awards-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-betplay-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-biometrics-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-cashier-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-chance-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-commissions-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-conciliation-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-credits-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-dashboard-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-download-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-dynamic-storage-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-external-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-external-wager-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-generic-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-hierarchies-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-invictus-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-login-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-lotteries-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-millonario-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-money-control-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-notifications-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-papelery-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-promotional-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-raffles-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-raspa-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-recharges-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +


          cd $(Build.SourcesDirectory)/iac-terragrunt/invictus/apps/aws-service-s3-policy/s3-users-policy
          terragrunt plan --all --non-interactive -no-color
          terragrunt apply -all --non-interactive -no-color

          cd $(Build.SourcesDirectory)
          find . -type d -name ".terragrunt-cache" -exec rm -rf {} +



          



      







          # cd $(Build.SourcesDirectory)
          # find . -type d -name ".terragrunt-cache" -exec rm -rf {} +
        displayName: "🚀 Apply Infrastructure"
        timeoutInMinutes: 120
        condition: succeeded()
        env:
          AWS_DEFAULT_REGION: $(AWS_DEFAULT_REGION)
          AWS_ACCESS_KEY_ID: $(AWS_ACCESS_KEY_ID_INVICTUS)
          AWS_SECRET_ACCESS_KEY: $(AWS_SECRET_ACCESS_KEY_INVICTUS)
