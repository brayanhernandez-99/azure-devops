jobs:
  - job: "create_tag_version_microfrontend"
    displayName: "🏷️ create tag version"
    pool:
      name: "Azure Pipelines"
      vmImage: ubuntu-latest

    steps:
      - checkout: self
        fetchDepth: 0
        persistCredentials: true

      - task: DownloadBuildArtifacts@0
        inputs:
          artifactName: "versions"
          downloadPath: "$(Pipeline.Workspace)/version-downloaded"
        displayName: "📥 Download Version"
        condition: succeeded()

      - task: Bash@3
        inputs:
          targetType: inline
          script: |
            set -e
            shopt -s nullglob globstar

            git config user.name "pipeline"
            git config user.email "pipelineUX@ux.technology"
            git fetch --tags

            FILES=$(echo "$(Pipeline.Workspace)/version-downloaded/versions"/**/version.json)
            for file in $FILES; do
              MS=$(jq -r .ms "$file")
              VERSION=$(jq -r .version "$file")                      
              TAG="${MS}-v${VERSION}"
              echo "🏷️ Tag a crear: ${TAG}"

              if git rev-parse "${TAG}" >/dev/null 2>&1; then
                echo "##vso[task.logissue type=warning]⚠️ El tag ${TAG} ya existe"
              else
                git tag -a "${TAG}" -m "Release ${TAG}"
                git push origin "${TAG}"
                echo "✅ Tag publicado: ${TAG}"
              fi
            done
        displayName: "🏷️ Create Tag Version"
        condition: succeeded()
