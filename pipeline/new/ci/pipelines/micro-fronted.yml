stages:
  - semantic_version_integration
  - microservices_development
  - build_development_antioquia

semantic_version_integration:
  jobs:
    - name: job1

microservices_development:
  jobs:
    - name: jobA

    - name: jobB
      depends_on: [jobA]

build_development_antioquia:
  jobs:
    - name: jobX

    - name: jobY
      depends_on: [jobX]

    #
    #
    #
    - ${{ if and(eq(parameters.release, 'aws-s3-new'), or(startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/'), startsWith(variables['Build.SourceBranch'], 'refs/heads/hotfix/'), startsWith(variables['Build.SourceBranch'], 'refs/heads/develop'), startsWith(variables['Build.SourceBranch'], 'refs/heads/staging'), startsWith(variables['Build.SourceBranch'], 'refs/heads/master'))) }}:
        - template: new/ci/template/semantic-version-integration.yml
          parameters:
            release: ${{ parameters.release }}

    - template: build/build-development-pr.yml
      parameters:
        release: ${{ parameters.release }}
        red: antioquia
